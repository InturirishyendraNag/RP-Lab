fn main() {
    let explicit_var: i64 = 100;
    let explicit_float: f32 = 2.718;
    println!("Explicit integer (i64): {}", explicit_var);
    println!("Explicit float (f32): {}", explicit_float);
}
